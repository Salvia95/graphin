# 재현 좌표 — 통합 에이전트 재베이스라인 (2026-09-01)

`graphin-explorer`를 `graphin-rag`로 통합하고(가이드 플러그인 0.6.0), 러너
격리를 고친 뒤의 재측정. 앞선 [같은 날 베이스라인](../2026-09-01-rag-baseline/findings.md)과
같은 저장소·같은 날이지만 **에이전트도 루브릭도 다른 런**이다.

| 좌표 | 값 |
|---|---|
| 수행 | 2026-09-01 14:21 KST 시작, 벽시계 합 91분 / 실측 경과 ~35분 (워커 3) |
| 소스 | graphin `e7f9999` + 미커밋 통합 변경 (`--worktree`) |
| 코퍼스 | 워크트리 스냅샷에서 측정 장치 제외 |
| 태스크셋 | 27태스크 × 3런 = 81, sha256 `5ca2a37b2b92…` |
| 에이전트 | 통합 `graphin-rag` sha256 `f67281fc2db3…` (이전 `b83715c342ad…`) |
| 합성 프롬프트 | sha256 `ec601e849315…` |
| 모델 | `sonnet` → claude-sonnet-5 |
| CLI | Claude Code 2.1.252 |
| 인덱스 | lexical-only, 워커별 스냅샷 사본 3 |
| 루브릭 | **1.3.0** (`RUN_COMPAT`이 1.3.0 하나로 리셋 — 옛 트랜스크립트는 도구 로스터가 달라 재채점 불가) |
| 비용 | $10.49 |

**직전 런과 나란히 읽으면 안 되는 이유**: 에이전트(통합)·태스크셋(19→27)·
도구 로스터(위임 도구 제거)가 동시에 바뀌었다. 방향은 읽되 델타를 인과로
읽지 않는다 — 이 저장소가 집계 평균으로 결론을 뒤집힌 적이 있다.
