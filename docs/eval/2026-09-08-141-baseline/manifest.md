# 런 스냅샷 매니페스트 — 2026-09-08 1.4.1 새 베이스라인

루브릭 1.4.0(스냅샷에서 `docs/eval` 절단)과 1.4.1(봉쇄 대칭 + 결정론 축)이
`RUN_COMPAT`을 두 번 리셋해 **비교 가능한 런이 0개**가 된 자리를 메우는 풀셋.
게이트 마커도 같은 이유로 만료돼 있었다(`2f05f8b` · 루브릭 1.3.5).

| 항목 | 값 |
|---|---|
| 수행일 | 2026-09-08 (81런, 오류 0, `--jobs 3`으로 실경과 23분) |
| 명령 | `scripts/eval-rag.py run --out <샌드박스>/rag-2026-09-08-141-baseline --runs 3 --jobs 3 --detach` |
| 소유자 지시 | "1번 부터 시작해줘"(제안 §1 = 1.4.1 풀셋 새 베이스라인), 이어서 "기록 쓰고 커밋해줘, 기준에 도달했으니 승격하자" |
| 바이너리 | `bin/graphin` v0.4.12-3-g8c0d392 — Go 트리는 `2f05f8b`(= v0.4.12 릴리스)와 바이트 동일, 버전 문자열만 HEAD |
| 에이전트 · 스킬 | `agents/graphin-rag.md` `c11b4614ad78` · `SKILL.md` `1fa366d41424` — D′와 같은 sha. 태스크셋 `5ca2a37b2b92` |
| 코퍼스 | `8c0d392`(HEAD, 트리 clean) — **`docs/eval/` 절단 적용된 첫 풀셋** |
| 검색 | lexical-only(`--ort-lib /nonexistent-ort`). 하이브리드는 별도 축 |
| 채점 | 루브릭 **1.4.1**로 먼저(→ 76/81), 승격 후 **1.4.2**로 재채점(→ 74/81). 채점 전용 변경이라 같은 트랜스크립트 |
| 모델 · CLI | sonnet · Claude Code 2.1.263 |
| 비용 | $10.15 · 벽시계 합 70분 |
| 게이트 | `score --gate 0.80` 통과, 마커 `commit 8c0d392` · mode **full** — 어떤 자리의 릴리스든 연다 |
| 산출 | 샌드박스 `~/projects/graphin-eval-sandbox/out/rag-2026-09-08-141-baseline/`(트랜스크립트 81), 이 디렉터리의 `report.md`(1.4.2) · `report-1.4.1.md`(승격 전) · `meta.json` |

**팔은 하나다.** grep 대조군은 1.4.1 대칭 이후 아직 안 돌았다 — 두 팔을 같은
봉쇄 조건에서 나란히 놓으려면 `--arm grep` 한 셋이 더 필요하다.
